{
  "name": "llm-on-the-can-bus",
  "version": "0.1.0",
  "description": "An LLM writes the car's CAN control program from scratch on every request — and a trusted safety supervisor makes sure it still can't do anything dangerous.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "node --test",
    "serve": "node scripts/serve.mjs",
    "bridge": "node scripts/unity-bridge.mjs"
  }
}
